<Window x:Class="Postec.Caisse.Views.AdministrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:Postec.Caisse.Views"
        Title="POSTEC - Administration"
        Width="1100" Height="700"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource CouleurFond}"
        FontFamily="{DynamicResource FamillePolice}"
        AutomationProperties.AutomationId="AdministrationView"
        AutomationProperties.Name="Administration">
    <TabControl Background="Transparent"
                AutomationProperties.Name="Onglets Administration">
        <TabItem Header="Gestion"
                 AutomationProperties.Name="Onglet Gestion"
                 AutomationProperties.AutomationId="TabGestion">
            <views:GestionUnifieeView x:Name="OngletGestion"/>
        </TabItem>
        <TabItem Header="Outils"
                 AutomationProperties.Name="Onglet Outils"
                 AutomationProperties.AutomationId="TabOutils">
            <views:OutilsUnifieeView x:Name="OngletOutils"/>
        </TabItem>
        <TabItem Header="Paramètres"
                 AutomationProperties.Name="Onglet Parametres"
                 AutomationProperties.AutomationId="TabParametres">
            <views:ParametresPeripheriquesView x:Name="OngletParametres"/>
        </TabItem>
    </TabControl>
</Window>
