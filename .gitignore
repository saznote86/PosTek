# ============================================================
#  Dépôt : code POSTEC uniquement.
#  Structure (restructuration du 2026-09-18) : le code vit à la
#  racine (desktop/, fiscal/, licence/, migration/, tests/,
#  tools/, docs/, donnees/) — le dossier postek/ historique a
#  disparu. Bases SQLite locales, backups et artefacts restent
#  hors versionnement.
# ============================================================

# Artefacts de build .NET
bin/
obj/

# Bases de données et données locales de l'app POSTEC
*.db
desktop/Postek.Caisse/bin/**/*.db
donnees/*
!donnees/*.json
imprimante.logique

# Backups de base à la racine
postek_*_backup_*.db
postek_demo_backup_*.db
*_backup_testraz_*.db
rapport_migration.json

# Logs et données d'exécution locales
postek_demarrage.log
raz_log.txt

# Python
__pycache__/
*.pyc
.venv/

# Données réelles de migration (client) : hors dépôt
export_leo2/

# Histoires d'agent
.aider*
